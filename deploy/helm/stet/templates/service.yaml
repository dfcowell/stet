apiVersion: v1
kind: Service
metadata:
  name: {{ include "stet.fullname" . }}
  labels:
    {{- include "stet.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "stet.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
